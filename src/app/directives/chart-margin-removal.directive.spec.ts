import { ChartMarginRemovalDirective } from './chart-margin-removal.directive';

describe('ChartMarginRemovalDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartMarginRemovalDirective();
    expect(directive).toBeTruthy();
  });
});
